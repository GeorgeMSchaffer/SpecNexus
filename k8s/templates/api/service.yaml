apiVersion: v1
kind: Service
metadata:
  name: sargent-nexus-api
  namespace: {{ .Values.namespace.name }}
spec:
  type: ClusterIP
  selector:
    app: sargent-nexus-api
  ports:
    - name: http
      port: {{ .Values.api.service.port }}
      targetPort: {{ .Values.api.service.targetPort }}
