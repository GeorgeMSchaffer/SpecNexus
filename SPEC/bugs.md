Bugs:
* Errant else { and else {} displayed as content on the change password screen
* Admin style pages should have a list view with a create organization button visible only to Site Admins.  The list view should display by default, clicking edit on existing record or editing current ones  should cause the form view to be loaded.
* On the Workflow page only a list of boards should be dispayed.
* There should also be a new Idea's page which is a list of ideas that were created by or are assigned to the user.  Clicking details should cause the Edit idea form to be loadedd.
* Remove all functionality related to weather and weather and the counter forecast that was just place holder code.
* Sign Out should be an icon on the left of the Username display in the header.
* The menu should be horizontal and under the main header.
* Let's rename the Admin Section to Setting 
* Admin functionality should be accessible via a gear icon, clicking on the gear should take the user to the Admin section. Here there should be links to various admin functionality as well as the ability for the user to update their information. There should also be links to Users, Boards and Status, Organization for Admin type user's depending on their permission.
* Each list page such as organizations, ideas, users, orgs should have a uniform search bard and pagination with the default of 25 items per page with the option to select 50, 100 or 250. Filtering should occur across all properties of an entitity. Meaning that for an org, the filter should search the same fields as are displayed as columns. city,state,phone and company
* Use the color rgb(33, 37, 41) for the entire header and make the Username font white.