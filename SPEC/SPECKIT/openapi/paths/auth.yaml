paths:
  /api/v1/auth/login:
    post:
      summary: Authenticate a user with globally unique email credentials
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ../components/schemas/auth.yaml#/LoginRequest
      responses:
        '200':
          description: Authenticated response
        '400':
          description: Malformed request body or invalid request shape
          content:
            application/problem+json:
              schema:
                $ref: ../components/schemas/common.yaml#/ValidationProblemDetails
        '401':
          description: Invalid credentials or missing authentication
          content:
            application/problem+json:
              schema:
                $ref: ../components/schemas/common.yaml#/ProblemDetails
        '403':
          description: Inactive account
          content:
            application/problem+json:
              schema:
                $ref: ../components/schemas/common.yaml#/ProblemDetails
        '429':
          description: Account locked out
  /api/v1/auth/me:
    get:
      summary: Get current authenticated user
      responses:
        '200':
          description: Current user summary
        '401':
          description: Missing, expired, or invalid authentication
          content:
            application/problem+json:
              schema:
                $ref: ../components/schemas/common.yaml#/ProblemDetails
  /api/v1/auth/change-password:
    post:
      summary: Change the current user's password
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ../components/schemas/auth.yaml#/ChangePasswordRequest
      responses:
        '204':
          description: Password changed
        '400':
          description: Malformed request body or invalid password policy
          content:
            application/problem+json:
              schema:
                $ref: ../components/schemas/common.yaml#/ValidationProblemDetails
        '401':
          description: Invalid current password or missing authentication
          content:
            application/problem+json:
              schema:
                $ref: ../components/schemas/common.yaml#/ProblemDetails
        '403':
          description: Authenticated caller is not allowed to change this password
          content:
            application/problem+json:
              schema:
                $ref: ../components/schemas/common.yaml#/ProblemDetails
