# Quickstart: Organizations and Users

1. As Site Admin, create an organization.
2. Verify the organization is provisioned with default statuses and one default board.
3. As Org Admin, create users inside the organization.
4. Verify duplicate email is rejected in the same organization and allowed in a different one.
5. Verify the last Org Admin cannot remove their own admin access.
